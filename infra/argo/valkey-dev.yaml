apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shurl-valkey-dev
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: oci://registry-1.docker.io/bitnamicharts/valkey
      targetRevision: "2.3.*"
      helm:
        valueFiles:
          - "$values/infra/helm/valkey.yaml"
          - "$values/infra/helm/valkey.dev.yaml"
    - repoURL: https://code.db.cafe/pauloo27/shurl.git
      targetRevision: dev
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: shurl-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
